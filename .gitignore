.env
api.txt
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
build/
dist/
# experiment outputs / scratch — never the prod graph
out/
*.jsonl
data/
!data/services/gold/*.jsonl

# --- PUBLISHED KNOWLEDGE GRAPH ---------------------------------------------
# The two grounded KG corpora under data/{kg,iol}/graph_stage3 are committed
# (force-added) so historians can clone & build — see README → "Open data".
# git keeps them tracked despite the broad data/ ignore above. These safety
# patterns just stop the rolling backups / built DB binary from being added:
data/*/graph_stage3/*bak*.jsonl
data/*/ladybug_db

# raw OCR source images (951MB) — regenerable, available on request / via Zenodo
iol_ocr_1860-1940/
