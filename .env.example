# Credentials resolve in this order: env vars → ./.env → ~/col_matching/.env →
# ~/textasdatacolonialofficelist/.env. You can leave this file unset and rely
# on the sibling repo's .env for a single credential source.

# NEO4J_URI=bolt://206.12.90.118:7687
# NEO4J_USER=neo4j
# NEO4J_PASSWORD=
# NEO4J_DATABASE=

# Extraction backfill (LLM staff-list extraction). Metered — keep the cap low.
# Rotate the key if it has ever been pasted into a chat/log.
# OPENROUTER_API_KEY=
# COL_BACKFILL_CAP_USD=5
# COL_BACKFILL_MODEL=deepseek/deepseek-v4-flash
# COL_BACKFILL_ESCALATE_MODEL=deepseek/deepseek-v4-pro
# OLLAMA_HOST=http://localhost:11434
